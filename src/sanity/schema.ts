import { product } from "./schemas/product";
import { artist } from "./schemas/artist";
import { siteSettings } from "./schemas/siteSettings";

export const schemaTypes = [product, artist, siteSettings];
